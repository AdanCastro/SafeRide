<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.uts.saferide">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Index">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroUsuario" />
        <activity
            android:name=".Login"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegistroConductor" />
        <activity
            android:name=".IndexAdmin"
            android:label="@string/title_activity_index_admin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".IndexUser"
            android:label="@string/title_activity_index_user"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".IndexConductor"
            android:label="@string/title_activity_index_conductor"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Perfil" />
        <activity android:name=".VerFotoU" />
        <activity android:name=".PerfilC" />
        <activity android:name=".VerFotoC" />
        <activity android:name=".InfoConductor" />
        <activity android:name=".ReportesUsuario" />
        <activity android:name=".InfoTarjeta"></activity>
    </application>

</manifest>